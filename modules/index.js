const {sum, substraction, multiplication, division} = require('./math');

const sum = sum(3, 5);
const substraction = substraction(3, 5);
const multiplication = multiplication(3, 5);
const division = division(3)(5);

console.log(operations, sum, substraction, multiplication, division);